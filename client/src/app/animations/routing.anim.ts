// import { trigger, animate, style, group, animateChild, query, stagger, transition } from '@angular/animations';

// export const RoutingAnim = trigger('RoutingAnim', [
  // transition('end => start', [ // Navigation occured, fade old out
  //  query('.contentWrapper', [
  //    style({ opacity: 1, }),
  //    animate('1s ease-in-out', style({ opacity: 0 }))
  //  ], { optional: true }),
  // ]),
  // transition('start => end', [ // Navigation ended, fade new item in
  //  query('.contentWrapper', [
  //    style({ opacity: 0.5, }),
  //    animate('0.5s ease-in-out', style({ opacity: 1 }))
  //  ], { optional: true }),
  // ]),
  // transition('end => start', [ // Navigation ended, fade new item in
  //  query(':leave', [
  //    style({ opacity: 0 }),
  //  ], { optional: true }),
  // ]),
// ]);
